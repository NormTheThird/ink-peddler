import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const MapScreen = ({ navigation }) => {
  return (
    <View>
      <Text style={{ fontSize: 48 }}>Map Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default MapScreen;